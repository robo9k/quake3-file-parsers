use crate::{
    lexer::TokenKind,
    parser::{CompletedMarker, Marker, Parser},
    syntax::SyntaxKind,
};

pub fn arenas(parser: &mut Parser) {
    todo!();
}

pub fn arena(parser: &mut Parser) {
    todo!();
}

pub fn key_value(parser: &mut Parser) {
    todo!();
}
